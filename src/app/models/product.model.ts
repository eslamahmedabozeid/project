export interface Product {
  ProductId: number; // Matches "ProductId" in JSON
  ProductName: string; // Matches "ProductName" in JSON
  ProductPrice: number; // Matches "ProductPrice" in JSON
  AvailablePieces: number; // Matches "AvailablePieces" in JSON
  ProductImg: string; // Matches "ProductImg" in JSON
}
